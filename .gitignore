# Obsidian per-machine UI state — config itself is tracked
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# Obsidian's own soft-delete folder
.trash/

# Symlink to the aziz-engineering-brain repo, which versions itself
notes/engineering-brain

.DS_Store
